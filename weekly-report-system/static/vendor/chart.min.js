/**
 * Chart.js v4 placeholder — closed-network deployment stub.
 *
 * REPLACE THIS FILE with the real Chart.js before production use:
 *   1. Download: https://cdn.jsdelivr.net/npm/chart.js@4.4.4/dist/chart.umd.min.js
 *   2. Save as: static/vendor/chart.min.js
 *
 * This stub provides a no-op Chart constructor so the page loads without errors.
 * The milestone bar chart on the dashboard will not render until replaced.
 */
(function (global) {
  'use strict';
  function Chart(ctx, config) {
    this.ctx = ctx;
    this.config = config;
    console.info('[Chart.js stub] Chart created — replace static/vendor/chart.min.js with real Chart.js');
  }
  Chart.prototype.update = function () {};
  Chart.prototype.destroy = function () {};
  global.Chart = Chart;
})(typeof window !== 'undefined' ? window : this);
